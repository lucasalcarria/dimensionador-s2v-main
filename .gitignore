# --- dados do usuário (NUNCA versionar / nunca sobrescrever) ---
clientes/
propostas/
tarifas_cache.json
padrao_modulo.txt
padrao_inversor.txt
pacotes_imagens.json
solicitacoes.json

# --- segredos do Google Drive (OAuth) — NUNCA versionar ---
google_oauth.json
google_token.json

# --- senha de acesso / chave de sessão — NUNCA versionar ---
acesso.json

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/

# --- build do executável (PyInstaller) ---
build/
dist/
*.spec

# --- planilha original de referência (12 MB, não versionar) ---
*.xlsm
*.xlsx

# --- graphify: cache e backups datados são derivados/transitórios ---
# (o grafo em si — graph.json, GRAPH_REPORT.md, etc. — continua versionado)
graphify-out/cache/
graphify-out/????-??-??/

# --- sistema / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
